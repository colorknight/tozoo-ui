# 默认不要写 VITE_DEV_PROXY_TARGET：Vite 会把 /member 代理到远程
# http://192.168.0.222:38172（与 vite.config.ts 里默认值一致）。
#
# 只有当你在本机跑 member-api、希望请求进本地进程时，再取消下面注释并改成你的本地地址/端口：
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:38172
#
# 不要配置 VITE_API_BASE（否则浏览器跨域直连后端）。若必须直连再取消下行并处理 CORS：
# VITE_API_BASE=http://192.168.0.222:38172

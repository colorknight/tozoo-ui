# 后端 API 地址（仅用于 Vite 开发/预览时代理目标）
# 不设置时默认为 http://localhost:8080
# 若后端在其他主机或端口，可设置例如：http://192.168.1.100:8080
# VITE_PROXY_TARGET=http://localhost:8080

# 可选：前端请求 API 时的基础 URL，留空则使用相对路径 /yansee（走代理，避免跨域）
# 仅当与前端同源的反向代理提供 /yansee 时留空即可
# VITE_API_BASE_URL=
